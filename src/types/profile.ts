/**
 * Canonical: `@sync/profile-contracts` (workspace package)
 * Do not duplicate shapes here — re-export only.
 */
export type {
  AccountRiskPublicStatus,
  AccountRiskReasonCode,
  AccountRiskStatus,
  AuthLoginResult,
  CurrentUser,
  PrivacyLevel,
  ProfileActivityItem,
  ProfileSummary,
  UpdateCurrentUserPayload,
} from '@sync/profile-contracts';
