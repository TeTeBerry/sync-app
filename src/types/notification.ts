/**
 * Canonical: `@sync/notification-contracts` (workspace package)
 * Do not duplicate shapes here — re-export only.
 */
export type {
  AppNotification,
  NotificationCategory,
  NotificationInteractionType,
  NotificationMeta,
  NotificationType,
} from '@sync/notification-contracts';
