/**
 * Canonical: `sync-app-backend/src/shared/chat/conversation-state.types.ts`
 * Do not duplicate shapes here — re-export only.
 */
export type {
  ConversationFlow,
  ConversationState,
  PublishDraftState,
  RecommendGateState,
} from '@sync/chat-contracts';

export { CONVERSATION_STATE_VERSION } from '@sync/chat-contracts';
