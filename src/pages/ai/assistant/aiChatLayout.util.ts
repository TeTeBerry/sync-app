/** Reserve space above tab bar so message list is not hidden behind fixed composer. */
export const AI_CHAT_COMPOSER_RESERVE_PX = 96;

export const isWeappRuntime = process.env.TARO_ENV === 'weapp';
