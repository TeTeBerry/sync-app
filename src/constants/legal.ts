import { SUPPORT_EMAIL } from './supportContact';

/**
 * 法律文档页展示信息（小程序运行时无 `process.env`，请在此直接配置）。
 * 上架前请改为营业执照上的主体名称与真实联系邮箱。
 */
export const LEGAL_OPERATOR_NAME = '本小程序运营者';

/** 隐私/法务联系邮箱，默认与客服邮箱一致 */
export const LEGAL_CONTACT_EMAIL = SUPPORT_EMAIL;
