import { ACTIVITY_GUEST_AVATAR_URLS } from './remoteImages';

/** Shared participant avatar pile for activity cards (design mock). */
export const ACTIVITY_GUEST_AVATARS: string[] = [...ACTIVITY_GUEST_AVATAR_URLS];
