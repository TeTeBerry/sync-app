/** Standard thumbnail widths for remote image CDN params. */
export const IMAGE_SIZE = {
  avatarSm: 48,
  avatarMd: 80,
  listThumb: 200,
  postImage: 400,
  featuredHero: 720,
  eventCardList: 200,
  eventCardDefault: 320,
} as const;
