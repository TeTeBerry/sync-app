/**
 * @deprecated Use `useEventMapController` inside EventMapCanvas instead.
 * Kept for backwards compatibility if imported elsewhere.
 */
export { useEventMapController as useEventMapPaint } from "./useEventMapController";
